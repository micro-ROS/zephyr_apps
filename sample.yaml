sample:
  name: Sample for MEMS sensors VL53L0X
tests:
  sample.sensor.vl53l1x:
    build_only: true
    depends_on: usb_device i2c vl53l1x 
    tags: usb
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Wait for DTR"
